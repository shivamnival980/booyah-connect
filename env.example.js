// ==========================================================================
// ENVIRONMENT CONFIGURATION TEMPLATE FOR BOOYAHCONNECT
// ==========================================================================
// Copy this file to `env.js` and replace the placeholder values with your
// actual frontend-safe Supabase credentials.
//
// `env.js` is included in .gitignore to prevent committing credentials.

window.ENV = {
  SUPABASE_URL: "YOUR_SUPABASE_PROJECT_URL_HERE",
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_PUBLISHABLE_KEY_HERE"
};
